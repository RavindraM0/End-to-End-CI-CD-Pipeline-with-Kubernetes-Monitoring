apiVersion: v1
kind: Service
metadata:
  name: devops-app
  namespace: devops
  labels:
    app: devops-app

spec:
  type: ClusterIP
  selector:
    app: devops-app

  ports:
  - name: http
    port: 80
    targetPort: http
    protocol: TCP
